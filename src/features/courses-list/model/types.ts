export type CourseListElement = {
  id: string;
  name: string;
  description: string;
};

export type CreateCourseListElement = {
  name: string;
  description: string;
};

export type DeleteCourseListElement = {
  id: string;
};
